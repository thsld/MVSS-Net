# Runtime deps for the GPU image (RunPod, CUDA). The default PyPI torch wheel
# is CUDA-enabled on linux/amd64 and bundles its own CUDA runtime; the host
# only needs an NVIDIA driver (RunPod provides one).
torch==2.12.1
torchvision==0.27.1
numpy==2.4.6

opencv-python-headless>=4.13,<4.14

fastapi==0.137.2
uvicorn[standard]==0.49.0
python-multipart>=0.0.9
